/**
 * Placeholder — migrations were removed in favour of synchronize: true
 * for dev. A fresh baseline migration will be generated before production
 * deployment. The health endpoint still references this array.
 */
export const EXPECTED_MIGRATION_NAMES: ReadonlyArray<string> = [];
